#include <stdio.h>

int main(){

    int numeros[5], maior, menor, soma = 0, media;

    for(int i = 0; i < 0; i++){
        printf("Digite um numero: ");
        scanf("\n%d", numeros[i])
    }

    for(int i = 0; i < 0; i++){
        if(numeros[i] > maior){
            maior = numeros[i];
        }

        if(numeros[i] < menor){
            maior = numeros[i];
        }
     media+= vetor[i];
 }

   media= media/5;

   for(int i = 0; i<5; i++){
     printf("\n%d", numeros[i]);
   }
  
   printf("\nO maior valor é: %d",maior);
   printf("\nO menor valor é: %d",menor);
   printf("\nA media de valores é: %d",media);
}
